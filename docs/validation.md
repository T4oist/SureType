# 0.2.1 界面更新

本轮简化中英文文案，改为左侧导航、圆角控件与实时预览；加入切页、按钮和浮层动效，并遵循 Windows 界面动画设置。

- 19 项回归检查通过。
- 新控件交互检查通过：下拉菜单宽度、位置/样式选择、滑块、开关、预览同步、切页动画结束和语言切换。
- 12 张中英文 WPF 渲染图通过检查（910 / 650 宽度）。
- 原生浮层测试通过：不抢焦点、点击穿透、五种位置、立即隐藏和淡出替换。
- 0.2.1 的跨进程生命周期检查因用户已在运行 0.2.0 而未执行。测试工具已修正，遇到已有实例不会再误报通过；0.2.0 的通过记录保留在下方。
- 0.2.0 快照已完成八小时后台验证，未见崩溃、心跳中断或持续资源增长趋势；不覆盖 0.2.1 的新界面和动效。其他未测项目保留在下方。

本轮界面渲染位于 artifacts/ui-021。已有用户进程未关闭，新版使用独立版本文件下载。

---
# 0.2.0 验证记录

记录日期：2026-09-10。状态：候选版，未达到完整稳定版发布门槛。

## 当前机器

- Windows 11 家庭版中文版，10.0.26200。
- AMD Ryzen 7 8845H，8 核 / 16 逻辑处理器，约 23.3 GiB 内存。
- 本机有 .NET 开发环境，因此不能据此证明“无 .NET 系统已实测”。

## 已通过

| 检查 | 结果 |
| --- | --- |
| 构建 | Debug 构建 0 错误、0 警告；Release 自包含发布成功 |
| 回归测试 | 19/19；覆盖持久化、原文件保护、错误反馈、状态标记、冷却去重、暂停恢复、串行采样、退出回调 |
| 位置计算 | 四角与鼠标旁；负坐标、边缘换侧、不同尺寸的几何测试通过 |
| 原生消息 | 成功返回 0 与调用失败区分；无效窗口、无响应测试窗口的 100ms 超时通过 |
| 实际浮层 | 当前桌面五种位置在工作区内，不抢前台焦点、命中测试穿透、立即隐藏、淡出替换通过 |
| 生命周期 | 隔离配置下首次打开、关闭到托盘、重复自启动静默退出、重复普通启动唤起通过 |
| 界面渲染 | 中英文 × 3 页面 × 2 宽度，共 12 张 WPF 离屏渲染；检查常规和窄窗口布局 |
| 打包 | 版本化自包含 exe、ZIP、SHA-256；发布脚本检查失败码与文件版本 |

生命周期测试直接运行应用代码，并启动实际产品 exe 检查跨进程单实例行为。原生窗口测试使用测试自行创建的窗口；它证明超时与浮层机制，不证明任一真实输入法识别准确。

## 兼容性矩阵

| 系统 / 场景 | 微软拼音 | 英文键盘 | 搜狗 | 微信输入法 |
| --- | --- | --- | --- | --- |
| Windows 11：记事本 | 未实测 | 未实测 | 未实测 | 未实测 |
| Windows 11：Edge / Chrome | 未实测 | 未实测 | 未实测 | 未实测 |
| Windows 11：VS Code | 未实测 | 未实测 | 未实测 | 未实测 |
| Windows 11：Office | 未实测 | 未实测 | 未实测 | 未实测 |
| Windows 11：聊天输入框 | 未实测 | 未实测 | 未实测 | 未实测 |
| Windows 10：上述应用 | 未实测 | 未实测 | 未实测 | 未实测 |

桌面交互工具启动与重启均返回 `windows sandbox failed: helper_unknown_error: setup refresh had errors`，未能完成真实应用键盘交互。没有将未核实安装或未执行的组合列为通过。

以下也仍待实测：真实输入模式连续切换 100 次、提示延迟 P95 ≤ 500ms、密码框、只读网页、真实全屏切换、混合 DPI 双屏、休眠恢复，以及无 .NET 的干净系统。

## 长期运行

使用 `tests/run-soak.ps1` 看护独立 Release 验证实例。配置和单实例名称与正常软件隔离；自动浮层关闭，避免在测试期间打扰工作。实际状态读取、UI Automation 焦点监听、前台事件、托盘与 WPF 调度器保持运行。

每分钟记录时间、累计 CPU、工作集、私有内存、句柄和线程数。外部看护检测超过三分钟无心跳、提前退出和超时。日志不包含输入文本、窗口标题、输入状态或应用路径历史。

### 八小时结果（2026-09-10 已复核）

0.2.0 隔离快照于 02:10:31–10:10:36（UTC+8）运行，累计 **28,804.65 秒**，超过八小时。482 条采样记录的最大心跳间隔为 **60.034 秒**，没有超过 180 秒的中断。未生成 failure.txt 或 watchdog-failure.txt；看护脚本报告正常完成，测试进程已退出。

| 指标 | 结果 |
| --- | --- |
| 全程平均 CPU | 0.0293%，按 16 个逻辑处理器归一化 |
| 前 30 分钟预热后平均 CPU | 0.0277% |
| 预热后私有内存范围 | 54.02–57.39 MiB |
| 第 2 / 第 8 小时平均私有内存 | 56.93 / 57.23 MiB |
| 第 2–8 小时句柄范围 | 670–676 |
| 第 2–8 小时线程范围 | 20–22 |
| 第 2 / 第 8 小时平均工作集 | 115.47 / 111.25 MiB |

预热后私有内存保持在约 57 MiB，小时均值小幅波动；句柄和线程数没有持续上升。此次采样未见明显泄漏趋势，但不能证明所有使用场景下都不存在泄漏。

结论：**此配置下的八小时后台运行检查通过**。自动浮层关闭，未验证真实输入法准确率、严格空闲 CPU、混合 DPI、无 .NET 系统，亦不覆盖 0.2.1 新动效。原始记录保留在 artifacts/soak-8h；跟进任务在复核后暂停。

复核原始结果时检查 `artifacts/soak-8h/result.json`、`samples.jsonl`、`failure.txt` 和 `watchdog-failure.txt`：
- 没有完成结果时，状态为进行中或未完成。
- 有失败记录或较大心跳间隔时，不能认定连续八小时通过。
- 完成后仍需分析预热后的内存、句柄趋势；平均 CPU 不是严格空闲场景指标。
- 即使后台长测通过，也不等于浮层在真实输入场景下长期稳定，或完整兼容性通过。

运行示例（先构建自包含测试程序）：

```powershell
./tests/run-soak.ps1 -TestExecutable ./artifacts/soak-runtime/SureType.Tests.exe -OutputDirectory ./artifacts/soak-8h -Minutes 480
```

机器需要持续运行；休眠、关机或重启会使连续验证无效。暂不更改系统电源设置。

## 发布判断

0.2.0 的功能实现与可自动执行的检查已形成候选版。完整稳定版还需补齐兼容性矩阵、实际延迟与连续切换、干净系统和开启浮层后的长期运行验收。不能仅凭此页的自动测试通过声明软件已经满足所有用户需要。
